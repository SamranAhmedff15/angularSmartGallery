import { OeuvreResponseMain } from './oeuvre-response-main';

describe('OeuvreResponseMain', () => {
  it('should create an instance', () => {
    expect(new OeuvreResponseMain()).toBeTruthy();
  });
});
